# Verbetering IP-adressen met list comprehensions
## Oefening
{% instructions %}

Pas je functie `read_ips()` aan zodat de omzetting van getallen gebeurt met een list comprehension indien dat nog niet het geval was.
